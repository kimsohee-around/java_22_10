0. day15 복습
컬렉션 프레임워크(프레임워크:자주 사용되는 기능들을 구현해 놓은 라이브러리) - java.util 패키지
 *  컬렉션(Collections) : 많은 양의 데이터를 저장하기 위한 자료구조를 구현한 라이브러리(클래스들)
 *  종류 : List(인덱스를 이용해서 순차적인 접근 가능)* 
 *        Set(집합.순서가 없고 값의 중복 허용 안함)
 *        Map(key-value 한쌍으로 데이터를 저장. 순서없고 값의 key는 중복 없음.)*
 *  List, Set, Map 등의 `인터페이스`로 구현됩니다.
 
중요한순서 : 리스트 > Map > Set

1. List와 Set 비교
           ㄴ Set 종류 : HashSet* , TreeSet 등이 있습니다. 
- 인덱스(순서)가 없을 때에는 데이터요소 접근은 Iterator `인터페이스` 구현체로 합니다.


2. Map :    ***key-value 한쌍으로 데이터를 저장. Map 장점 : key를 이용한 검색이 됩니다.
   ㄴ 데이터요소에 순서가 없습니다.
   ㄴ key값은 중복값을 허용 안함.
   ㄴ 종류는 HashMap*, TreeMap
 메소드
 put(key,value)  : 동일한 key 값으로 put 실행할 때에는 항목추가 아니고 value 변경
 get(key)   : value를 가져옵니다.
 remove(key) 또는 remove(key,value)
 containsKey(key) : map에 있는 key 에 입력한 key 값이 있는지.  

3. TreeSet과 TreeMap
Set 과 Map은 순서가 없다. 즉 인덱스를 사용할 때와 같이 위치를 지정해서 가져오지 못한다.
그러나 데이터를 저장할 때 미리 정렬되게 할수는 있다. 
TreeSet 은 저장되는 값을 정렬하고
TreeMap 은 key 값으로 정렬한다.

소스 : AddTwoNum.java 참고




   	 